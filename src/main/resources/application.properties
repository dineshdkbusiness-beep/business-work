server.port=${PORT:8080}

# PostgreSQL (Render)
spring.datasource.url=jdbc:postgresql://dpg-d7nmghm8bjmc738uglk0-a.oregon-postgres.render.com:5432/line_business_db?sslmode=require
spring.datasource.username=root
spring.datasource.password=5Ng7NfSUmPiy2Hran1cY0QK2jXy4J4Rt
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Formatting & logging
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

# Fix for data.sql timing
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# Fix for PostgreSQL LOB issue
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Disable devtools restart
spring.devtools.restart.enabled=false